# ShinyUL : R Shiny for applying Unsupervised Learning methods. 

ShinyUL is an R package that provides a GUI (Graphical User Interface) to easily perform Data Mining using unsupervised learning techniques like Clustering with K-Means and dimensionnality reduction using Principal Component Analysis (PCA), and -Distributed Stochastic Neighbor Embedding (t-SNE).
